# AI Multi-Tool Kit FUI Importer

Editor-only Unity importer for `.fui` packages exported from AI Multi-Tool Kit Figma plugin.

Open: `Tools → AI Multi-Tool Kit → FUI Importer`.

After import you can delete this folder. Generated `UXML`, `USS`, textures and fonts remain standard Unity assets.
